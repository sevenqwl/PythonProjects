$(function(){

    // alert($('#box').html());
    // alert($('#box').text());

    // $('#box').html('<em>www.li.cc</em>');  // 替换HTML内容，HTML会自动解析
    // $('#box').text('<em>www.li.cc</em>');  // 替换文本内容，有HTML会自动转义

    // alert($('input').val());
    // $('input').val('beifengwang');

    // $('input').val(['男', '女', '编程']);  // 单选框、复选框、下拉菜单默认选中
    // $('select').val(['3']);

    // $('div').attr('title', '我是域名');
    // $('div').attr({
    //     'title': '我是域名',
    //     'class': 'red',
    // });

    // $('div').attr('title', function(index, value){
    //     return '之前的value是' + value + '， 现在的value是' + '我是' + index + '号域名' ;
    // });

    // $('div').html(function(index, value){
    //     return value + '<em>www.li.cc</em>';
    // });

    // $('div').removeAttr('title');


});
